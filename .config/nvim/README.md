# Keybindings
[TOC]
## Treesitter/LSP
```
gl - Show (error) message
gd - Go to definition
gr - Go to references
```

## Plugins
AutoPairs
`"<C-p><C-p>"` In insert mode add the first wrapper "(" then CTRL P twice to select the closing position
