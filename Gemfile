# frozen_string_literal: true

# This is the global gemfile
# To install it run bundle install --system
source "https://rubygems.org" do
  #System Utils
  gem 'bropages'

  # Ruby Dev
  gem 'rspec'
  gem 'pry'
  gem 'pry-nav'
  gem 'reek'
  gem 'guard-rspec', require: false
  gem 'rb-readline' # for guard

  gem 'contracts'
  gem 'yard-contracts'
end
